#include <stdio.h>

int main() 
{
    int n;
    printf("enter  n");
    scanf("%d",&n);
    if(n>0)
    {
        printf("the number is positive");
    }
    else
    {
        printf("the number is negative");
    }
}
    
