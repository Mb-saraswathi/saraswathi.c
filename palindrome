#include <stdio.h>

int main() 
{
int n,rev=0,rem,org;
printf("enter n:");
scanf("%d",&n);
org=n;
while(n!=0)
{
    rem=n%10;
    rev=rev*10+rem;
    n=n/10;
}
if (org==rev)
printf(" %d it is a palindrome",org);
else
printf(" %d it is not a palindrome",org);
}
